import React from 'react'; //Чтобы заработал JSX

const Header = (props) => {
    return (
      <h1>ЗДЕСЬ БУДЕТ ШАПКА САЙТА</h1>
    );
}

export default Header; // Даем разрешение на подключение другим файлам